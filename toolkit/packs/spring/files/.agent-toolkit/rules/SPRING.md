# Spring Rules
- Prefer constructor injection and clear service boundaries.
- Validate external DTOs, authorize server-side, and avoid exposing persistence entities as unstable API contracts.
- Keep transactional boundaries explicit.
- Avoid N+1/lazy-loading surprises and test repository/service behavior.
- Follow installed Spring Boot version semantics.
