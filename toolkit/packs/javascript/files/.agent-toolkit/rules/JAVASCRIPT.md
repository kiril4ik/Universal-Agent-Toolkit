# JavaScript Rules
- Follow project runtime and module targets.
- Prefer `const`, explicit data flow, early returns, and small focused functions.
- Handle promise rejection and async concurrency intentionally.
- Validate external inputs.
- Avoid prototype mutation, accidental globals, and unnecessary mutation.
- Use project lint, format, and test tooling.
