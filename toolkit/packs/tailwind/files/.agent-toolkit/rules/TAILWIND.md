# Tailwind CSS Rules
- Follow the installed Tailwind major version and existing token/theme setup.
- Reuse design tokens/components before introducing arbitrary values.
- Keep responsive and focus/hover/disabled states intentional.
- Respect reduced motion and accessible contrast/focus indicators.
- Avoid unreadable duplication; extract components/utilities only when reuse is real.
