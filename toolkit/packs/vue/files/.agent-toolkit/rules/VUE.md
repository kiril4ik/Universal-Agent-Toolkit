# Vue Rules
- Follow the installed Vue/Nuxt version and existing Composition/Options API conventions.
- Keep state derived rather than duplicated; use computed values for derivation.
- Use watchers for side-effect synchronization, not ordinary computed state.
- Keep props/events explicit and typed when TypeScript is used.
- Preserve accessibility and verify important flows in browser tests.
