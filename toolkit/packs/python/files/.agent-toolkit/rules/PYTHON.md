# Python Rules
- Follow the project's Python version and formatter/linter/type checker.
- Use type hints for public interfaces and complex data shapes.
- Prefer explicit dependency boundaries and context managers for resources.
- Never use bare `except` to hide failures.
- Validate external data and keep secrets out of logs.
- Use project-local dependency management and lock files when the project provides them.
